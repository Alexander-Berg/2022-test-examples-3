{
    "scheduler" = {
        "max_running_operation_count_per_pool" = 20;
        "environment" = {
            "TMPDIR" = "$(SandboxPath)";
            "PYTHON_EGG_CACHE" = "$(SandboxPath)/.python-eggs";
            "PYTHONUSERBASE" = "$(SandboxPath)/.python-site-packages";
            "PYTHONPATH" = "$(SandboxPath)";
            "HOME" = "$(SandboxPath)";
        };
    };
}
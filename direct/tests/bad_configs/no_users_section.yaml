mysql_version: '5.6'

hosts_aliases:
  local:
    hosts: [ localhost ]

grants:
  - user: 'root'
    host: 'local'
    object_type: 'TABLE'
    priv_level: [ '*.*' ]
    priv_type: [ ALL ]
    grant_option: True
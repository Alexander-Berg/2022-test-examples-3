# Файл описывает конфигурацию запуска стрельб в лунапарке.
# В Файле в обязательно порядке должны присутствовать следующие поля:
# login - логин из под которого будем запускать стрельбы
# tasks - массив задач
# каждая задача описывается полями:
# *id - id стрельбы в лунапарке, которую мы будем повторно запускать использовав новые патроны
# *ammo - команда генерирующая патроны, предполагается, что генератор выводит результаты своей работы в stdout

login: vananos

tasks:
    - id: 1720055 
      ammo: "./generators/bids_get.pl --count 10000"

[test-juggler-check]
execution_interval=60
execution_timeout=60
command=export TEST_FILE=/tmp/test-juggler-check-file; ls "$TEST_FILE" > /dev/null 2>&1 && echo "2;Not OK, test file $TEST_FILE exists" || echo "0;OK, test file $TEST_FILE doesn't exist"

# approved by lena-san
# на контейнере нагрузочного тестирования сменить конфигурационный пакет
# когда: до выкладки остальных пакетов
sudo dpkg --purge yandex-direct-conf-test
sudo apt-get update
sudo apt-get install yandex-direct-conf-testload 

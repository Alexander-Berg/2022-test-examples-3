select exists(select distinct exists(select distinct y, x2, z from coords where company_id = ?), a.b, a.z, z.x, a.y from coords2), a.d, exists(select distinct exists(select distinct z, y, x1 from coords where company_id = ?), a.b, a.z, z.x, a.y from coords2) from tbl1 inner join tbl2 on tbl1.id = tbl2.id where (((tbl.a = tbl.b or tblb = tbl.c) and (tbl3.a <> tbl4.y) || tbl5.x is null) or tbl4.id = ? or tbl4.id like ? or (tbl.z in (...)
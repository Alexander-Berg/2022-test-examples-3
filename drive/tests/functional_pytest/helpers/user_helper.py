class User:

    def __init__(self, login, password, token, backend_client):
        if login:
            self.login = login
        else:
            pass
        if login:
            self.password = login
        else:
            pass

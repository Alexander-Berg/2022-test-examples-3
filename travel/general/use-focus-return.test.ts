import { renderHook } from '@testing-library/react';

import { useFocusReturn } from './use-focus-return';

describe.skip('hooks/use-focus-return', () => {
    test('TODO', () => {
        // TODO

        renderHook(() =>
            useFocusReturn({
                active: true,
            }),
        );
    });
});

import { renderHook } from '@testing-library/react';

import { useMergeCallback } from './use-merge-callback';

describe.skip('hooks/use-merge-callback', () => {
    test('TODO', () => {
        renderHook(() => useMergeCallback());
    });
});

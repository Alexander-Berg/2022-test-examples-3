package ru.yandex.tours.hotels

import org.scalatest.{Ignore, Matchers, WordSpecLike}

@Ignore
class HotelIOSpec extends WordSpecLike with Matchers {

  "HotelIO" should {
    "Parse current hotels without warnings" in {
//      val x = HotelIO.loadFromFile(new File("tours-data/data/hotels"))
//      x.foreach(hotel => {
//        Hotel.parse(hotel)._2 should have size 0
//      })
    }
  }

}

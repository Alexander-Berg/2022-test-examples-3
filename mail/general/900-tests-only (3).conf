import os

TESTS_RUNNING = os.environ.get("TESTS_RUNNING")
if TESTS_RUNNING == "1":
    LETTER_ATTACHMENTS_MAX_COUNT = 2
    LETTER_ATTACHMENTS_MAX_SIZE = 5 * 1024  # 5KB

    USER_TEMPLATE_VARIABLES_MAX_COUNT = 5
    USER_TEMPLATE_VARIABLE_MAX_LENGTH = 16
    USER_TEMPLATE_VARIABLE_VALUE_MAX_LENGTH = 128

    SILENTLY_DROP_SENTS_FOR_NEW_UNTRUSTY_ACCOUNTS = False

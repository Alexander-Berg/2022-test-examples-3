{
  "type": "object",
  "properties": {
    "tag_id": {
      "description": "identifiers of the current tag for current user",
      "type": "integer"
    },
    "type": {
      "description": "tag type: system or user",
      "type": "string"
    },
    "name": {
      "description": "identifier of the current user state when last change for this tag was made",
      "type": "string"
    },
    "contacts_count": {
      "description": "number of contacts with this tag",
      "type": "integer"
    },
    "revision": {
      "description": "identifier of the current tag state, increments by every change",
      "type": "integer"
    }
  },
  "required": [
    "tag_id",
    "type",
    "name",
    "contacts_count",
    "revision"
  ]
}

{
  "type": "object",
  "properties": {
    "tag_id": {
      "description": "identifiers of the current tag for current user",
      "type": "integer"
    },
    "revision": {
      "description": "identifier of the current tag state, increments by every change",
      "type": "integer"
    }
  },
  "required": [
    "tag_id",
    "revision"
  ]
}

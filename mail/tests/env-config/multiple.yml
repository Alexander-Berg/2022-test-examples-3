environment: env.local
config:
    system:
        choose_by_env:
            local:
                a : aval
                b: bval
            other: not array
    modules:
        choose_by_env:
            local: fval
            other: hval

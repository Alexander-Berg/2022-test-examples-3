environment: env.local
config:
    system:
        choose_by_env:
            local:
            -   a : av
            -   b : bv
            -   c : bc
            other: not array
    modules:
        choose_by_env:
            other:
            -   a : av
            -   b : bv
            -   c : bc
            local: not array

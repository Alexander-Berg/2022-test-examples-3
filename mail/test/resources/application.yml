micronaut:
  http:
    services:
      so:
        url: http://checkform-testing.so.yandex-team.ru
        health-check: false
        logger-name: so.client
        read-timeout: 3
        read-idle-timeout: 1
        connect-timeout: 5
        num-of-threads: 1
        pool:
          enabled: true

datasources:
  master:
    url: jdbc:postgresql://localhost:${pglocal.database.port}/${test.database.name}
    username: testuser
    password: ""
    initializationFailTimeout: -1
    autoCommit: false

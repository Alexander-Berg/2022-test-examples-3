version: '3.4'
services:
    xivamob_server:
        build:
            context: $xivamob
            network: host
            args:
                deploydir: /deploy
                installdir: /install
        network_mode: "host"

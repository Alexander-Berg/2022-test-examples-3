'use strict';

const el = document.createElement('div');
el.innerText = '{"prefetched":{"models":{}},"Config":{"connection_id":"TEST_CONNECTION_ID"}}';
el.id = 'insert-js';
document.body.appendChild(el);

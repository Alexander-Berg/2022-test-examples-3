# Квота на все запросы, не попадающие под другие квоты
default:
  quota: 1000

# Квота на каждый src tvm_id по 100 рпс
balance:
  quota: 100
  by:
    tvm_payments_test: false  # Размножить квоту по tvm id, false - tvm не обязательынй в запросе

lpm:
  quota: 100
  #by:
  #  tvm_payments_test: false

test:
  quota: 1  # Просто квота в 1 рпс

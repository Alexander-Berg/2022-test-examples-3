import pytest

__author__ = 'aokhotin'

import socket


def test_one():
    pass

#@pytest.mark.parametrize(('index',), [(1,), (2,), (3,), (4,), (5,), (6,), (7,)])
#def test_host_name(index):
#    assert '%s%s' % (socket.gethostname(), index % 2) == 'vegas.yandex.ru1'

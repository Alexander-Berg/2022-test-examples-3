urls: urls
headers:
  h-authorization: authorization
  tag_user: x-yandex-icookie
export:
  - h-authorization
  - tag_user

router:
  - map: urls
    data:
      - key: /api
        quota: test
      - key: /collections/api
        quota: test

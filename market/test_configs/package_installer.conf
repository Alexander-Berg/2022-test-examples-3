# This config is used only for tests. Package installer is runtime cloud
# specific plugin, the real config is in tmpl directory.

[general]
download_dir = packages/download
download_dir_async = packages/download_async
versions_dir = versions
install_dir = install
packages_dir = packages
uc_bin = uc
decompress_threads = 1
force_report_restart_file = /var/lib/search/report_force_restart

[dists]
report =

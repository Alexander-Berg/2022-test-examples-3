'use strict';

import Logo from 'spec/page-objects/Logo';

export default {
    suiteName: 'Logo',
    selector: `${Logo.root}`,
    // eslint-disable-next-line @typescript-eslint/no-empty-function
    capture() {},
};

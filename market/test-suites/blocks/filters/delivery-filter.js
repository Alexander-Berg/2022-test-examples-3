import FilterDeliveryInterval from '@self/platform/spec/page-objects/FilterDeliveryInterval';

export default {
    suiteName: 'DeliveryInterval',
    selector: [
        FilterDeliveryInterval.root,
    ],
    capture() {},
};

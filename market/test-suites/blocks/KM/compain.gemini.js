import ComplainForm from '@self/platform/spec/page-objects/components/ComplaintForm';

export default {
    suiteName: 'Complain',
    selector: [ComplainForm.root, ComplainForm.footer],
    capture() {},
};

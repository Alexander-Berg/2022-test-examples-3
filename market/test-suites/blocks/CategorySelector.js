import SearchPrepack from '@self/platform/spec/page-objects/search-prepack';


export default {
    suiteName: 'CategorySelector',
    selector: `${SearchPrepack.root} > div > div`,
    capture() {},
};

Core {
    Log {
        Level: DEBUG

        # comment for writting to stdout
        Target {
            FilePath: "logs/cpp-test-service/cpp-test-service.log"
        }
    }

    Identity {
        Name: "cpp-test-service"
    }

    TraceLog {
        Target {
            FilePath: "logs/cpp-test-service/cpp-test-service-trace.log"
        }
    }
}

const HOST = 'http://sovetnik-filter.yandex.net';

const ROUTE = /.*/;

const METHOD = 'post';
const RESPONSE = {
    show: 'yes',
};

module.exports = {
    host: HOST,
    route: ROUTE,
    method: METHOD,
    response: RESPONSE,
};

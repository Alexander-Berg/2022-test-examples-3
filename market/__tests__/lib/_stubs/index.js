'use strict';

const settings = require('./settings');
const requests = require('./requests');
const responses = require('./responses');

module.exports = {
    settings,
    requests,
    responses
};

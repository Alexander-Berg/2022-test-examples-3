Replicas {
    Proxy: "arnold"
    Sync: true
    OptimizeFor: "lookup"
    PrimaryMedium: "ssd_blobs"
    TabletCellBundle: "market-datacamp-testing"
    EnableReplicatedTableTracker: true
}

Replicas {
    Proxy: "hahn"
    Sync: false
    OptimizeFor: "lookup"
    PrimaryMedium: "ssd_blobs"
    TabletCellBundle: "market-datacamp-testing"
    EnableReplicatedTableTracker: true
}

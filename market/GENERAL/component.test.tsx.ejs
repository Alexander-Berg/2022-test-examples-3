import React from 'react';
import { render } from '@testing-library/react';

import { <%- nameClassCase %> } from './<%- nameClassCase %>';

describe('<<%- nameClassCase %> />', () => {
  it('renders without errors', () => {
    render(<<%- nameClassCase %> />);
  });
});

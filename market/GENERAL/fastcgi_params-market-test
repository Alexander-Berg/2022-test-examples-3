fastcgi_pass unix:/tmp/xscript-passport-testing.sock;
        fastcgi_split_path_info ^(.+\.xml)(.*)$;
        fastcgi_index index.xml;
        fastcgi_param PATH_INFO          $fastcgi_path_info;
        fastcgi_param QUERY_STRING       $query_string;
        fastcgi_param REQUEST_METHOD     $request_method;
        fastcgi_param SCRIPT_FILENAME    $root_dir$fastcgi_script_name;
        fastcgi_param SCRIPT_NAME        $fastcgi_script_name;
        fastcgi_param REQUEST_URI        $request_uri;
        fastcgi_param DOCUMENT_ROOT      $root_dir;
        fastcgi_param SERVER_PROTOCOL    $server_protocol;
        fastcgi_param GATEWAY_INTERFACE  CGI/1.1;
        fastcgi_param SERVER_SOFTWARE    nginx/$nginx_version;
        fastcgi_param REDIRECT_STATUS 404;
        fastcgi_param REMOTE_ADDR        $remote_addr;
        fastcgi_param REMOTE_PORT        $remote_port;
        fastcgi_param SERVER_ADDR        $server_addr;
        fastcgi_param SERVER_PORT        $server_port;
        fastcgi_param SERVER_NAME        $server_name;
        fastcgi_param XSCRIPT_SOCIAL_HOST "//social-test.yandex.ru";
        fastcgi_param XSCRIPT_PASSPORT_HOST "passport-test.yandex.ru";
        fastcgi_param XSCRIPT_PASS_HOST "pass-test.yandex.ru";
        fastcgi_param XSCRIPT_REQUEST_ID $request_id;
        fastcgi_param XSCRIPT_ZINGER_DIR $project_dir;
        fastcgi_param XSCRIPT_PROJECT_DIR $project_dir;
        fastcgi_param XSCRIPT_YA_SMS_HOST "phone-passport-test.yandex.ru";
        fastcgi_param XSCRIPT_PHONE_VALIDATOR_HOST "//phone-passport-test.yandex.ru";
        fastcgi_param XSCRIPT_BLACKBOX_HOST "pass-test.yandex.ru";
        fastcgi_param XSCRIPT_PASSPORT_INTERNAL_HOST "passport-test-internal.yandex.ru";
        fastcgi_param XSCRIPT_AVATARS_HOST "//avatars.mdst.yandex.net";

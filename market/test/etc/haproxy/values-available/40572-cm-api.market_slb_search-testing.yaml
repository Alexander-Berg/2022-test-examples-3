# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_triple_waterfall.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          "check observe layer7"
      on_error:       fastinter
    globals:
      grpc:           true
      service_name:   cm-api.tst.vs.market.yandex.net
      service_port:   80

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    be1:
    - name: YP@testing-market-mbo-category-iva@local
      grpc_port_offset: 2
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-mbo-category-sas@local
      grpc_port_offset: 2
      dns_resolvers: yandex-ns
      resolve: false
    be3:
    - name: YP@testing-market-mbo-category-iva
      grpc_port_offset: 2
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-mbo-category-sas
      grpc_port_offset: 2
      dns_resolvers: yandex-ns
      resolve: false

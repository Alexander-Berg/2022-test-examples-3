# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter
    globals:
      service_name:   clickhouse-public.tst.vs.market.yandex.net
      service_port:   80
      ping_expect:    "status 200"
      timeout_server: 120s
      timeout_client: 120s
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
      - name: "YP@market-infra_clickhouse-public_testing.clickhouse@sas"
        dns_resolvers: "yandex-ns"
        resolve: false
      - name: "YP@market-infra_clickhouse-public_testing.clickhouse@vla"
        dns_resolvers: "yandex-ns"
        resolve: false
        sort_order: shuffled


# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    be_defaults:
      check:          "check observe layer7"
      on_error:       fastinter
    be_globals:
      timeout_queue:   500ms
      timeout_connect: 10ms
    be3_globals:
      timeout_connect: 50ms
    globals:
      service_name:   uaas.tst.vs.market.yandex.net
      service_port:   80
      ping_expect:    "status 200"
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
      - name: "uaas.search.yandex.net"

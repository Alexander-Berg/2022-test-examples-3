# Generated by le087

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          "check ssl verify none observe layer7"
      on_error:       fastinter
    globals:
      service_name:   market-click2.tst.vs.market.yandex.net
      listen_port:    80
      service_port:   80
      ping_expect:    rstring "ok\n"
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
    - name: YP@testing-market-clickdaemon-sas
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-clickdaemon-iva
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-clickdaemon-vla
      sort_order: shuffled
      dns_resolvers: yandex-ns
      resolve: false

# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter
    globals:
      listen_port:    80
      ping_expect:    "status 200"
      service_name:   mbo-cms-exporter.tst.vs.market.yandex.net
      service_port:   443
      timeout_server: 1h
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
    - name: YP@testing-market-mbo-cms-exporter-vla
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-mbo-cms-exporter-sas
      sort_order: shuffled
      dns_resolvers: yandex-ns
      resolve: false

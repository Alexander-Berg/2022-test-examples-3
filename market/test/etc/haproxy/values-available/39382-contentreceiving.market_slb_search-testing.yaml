# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb_triple_waterfall.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter

    be1_globals:
      timeout_connect: 25

    globals:
      grpc:           true
      service_name:   "contentreceiving.tst.vs.market.yandex.net"
      service_port:   80
      ping_expect:    "status 200"
      timeout_server: "600s"
      timeout_client: "600s"

    meta:
      monrun_ignore:  true

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    be1:
    - name: YP@testing-market-contentreceiving-api-vla@local
      grpc_port: 4201
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-contentreceiving-api-sas@local
      grpc_port: 4201
      dns_resolvers: yandex-ns
      resolve: false
    be3:
    - name: YP@testing-market-contentreceiving-api-vla
      grpc_port: 4201
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-contentreceiving-api-sas
      grpc_port: 4201
      dns_resolvers: yandex-ns
      resolve: false

# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter
    globals:
      service_name:   wh-network-monitor.tst.vs.market.yandex.net
      listen_port:    80
      service_port:   443
      ping_expect:    "status 200"
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
      - name: "N@testing_market_wh_network_monitor_vla"
        sort_order: shuffled

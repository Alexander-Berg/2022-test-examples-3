# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_triple_waterfall.tpl

# Значения, доступные в шаблоне
values:
  default:
    be_defaults:
      check:          check observe layer7
      on_error:       fastinter
    grpc_be_defaults:
    globals:
      grpc:           true
      service_name:   access-puller.tst.vs.market.yandex.net
      listen_port:    80
      service_port:   443
      ping_expect:    "status 200"
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    be1:
    - name: YP@testing-market-access-puller-vla@local
      sort_order: shuffled
      dns_resolvers: yandex-ns
      resolve: false
    be3:
    - name: YP@testing-market-access-puller-vla
      sort_order: shuffled
      dns_resolvers: yandex-ns
      resolve: false

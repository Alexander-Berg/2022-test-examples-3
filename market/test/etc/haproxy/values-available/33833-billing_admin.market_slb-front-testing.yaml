# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter
      ssl:            ssl verify none
    globals:
      service_name:   billing-admin.tst.vs.market.yandex.net
      ping_vhost:     billing-admin.tst.market.yandex.ru
      listen_port:    80
      service_port:   443
      ping_expect:    "status 200"
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
    - name: YP@testing-market-front-pi-sas
      port_offset: 7
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-front-pi-vla
      port_offset: 7
      sort_order: shuffled
      dns_resolvers: yandex-ns
      resolve: false

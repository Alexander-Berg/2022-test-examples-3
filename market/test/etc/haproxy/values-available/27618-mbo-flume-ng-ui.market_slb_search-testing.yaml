# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          "check observe layer7"
      on_error:       fastinter
    globals:
      service_name:   mbo-flume-ng-ui.tst.vs.market.yandex.net
      service_port:   80
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
    - name: YP@testing-market-mbo-flume-ng-mbo-ui-iva
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-mbo-flume-ng-mbo-ui-vla
      sort_order: shuffled
      dns_resolvers: yandex-ns
      resolve: false

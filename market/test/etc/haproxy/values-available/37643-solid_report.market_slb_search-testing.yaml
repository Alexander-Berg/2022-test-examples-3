# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter
      inter:          2000

    globals:
      client_counters_max_rrate: 200
      client_counters_size: 10k
      display_port:   17051
      host_counters_size: 10k
      http_reuse:     aggressive
      service_port:   17050
      sticky:         true
      timeout_queue:  5s
      service_name:   "solid-report.tst.vs.market.yandex.net"

    meta:
      monrun_ignore:  true

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
    - name: N@test_report_market_solid_sas
    - name: N@test_report_market_solid_vla

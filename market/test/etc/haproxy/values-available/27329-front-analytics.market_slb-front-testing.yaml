# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          "check ssl verify none observe layer7"
      on_error:       fastinter
    globals:
      service_name:   front-analytics.tst.vs.market.yandex.net
      listen_port:    80
      service_port:   443
      ping_expect:     rstring "(^0;(ok|OK|Ok)|pong)"
      ping_vhost:      heartbeat.market.yandex.ru
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
    - name: YP@testing-market-front-pi-sas
      port_offset: 3
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-front-pi-vla
      port_offset: 3
      sort_order: shuffled
      dns_resolvers: yandex-ns
      resolve: false

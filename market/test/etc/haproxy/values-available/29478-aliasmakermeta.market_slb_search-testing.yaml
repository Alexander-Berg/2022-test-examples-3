# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter
    globals:
      service_name:   aliasmakermeta.tst.vs.market.yandex.net
      service_port:   35234
      ping_expect:    "status 200"
      timeout_client: 300s
      timeout_server: 300s
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
    - name: YP@testing-market-alias-maker-meta-sas
      dns_resolvers: yandex-ns
      resolve: false
    - name: YP@testing-market-alias-maker-meta-vla
      sort_order: shuffled
      dns_resolvers: yandex-ns
      resolve: false

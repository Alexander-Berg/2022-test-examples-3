# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check ssl verify none observe layer7
      on_error:       fastinter

    globals:
      service_name:   "mbi-partner-es-campaigns.tst.vs.market.yandex.net"
      service_port:   9200
      ping:           "/"
      ping_expect:    "status 401"

    meta:
      monrun_ignore:  true

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
      - name: "sas-rt2qkci4e25d9vjh.db.yandex.net"
      - name: "vla-opln1qp9q56pb2ss.db.yandex.net"

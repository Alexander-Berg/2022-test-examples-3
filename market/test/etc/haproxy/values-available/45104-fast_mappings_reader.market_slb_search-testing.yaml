# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb_triple_waterfall.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter

    be1_globals:
      timeout_connect: 25

    globals:
      service_name:   "fast-mappings-reader.tst.vs.market.yandex.net"
      service_port:   80
      ping_expect:    "status 200"
      grpc: true
      grpc_ssl: false

    meta:
      monrun_ignore:  true

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    be1:
      - name: "YP@testing_market_fast-mappings-reader.fast-mappings-reader@local"
        dns_resolvers: yandex-ns
        resolve: false
        grpc_port_offset: 2
    be3:
      - name: "YP@testing_market_fast-mappings-reader.fast-mappings-reader"
        dns_resolvers: yandex-ns
        resolve: false
        grpc_port_offset: 2

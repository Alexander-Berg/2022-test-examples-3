# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb_triple_waterfall.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter

    be1_globals:
      timeout_connect: 25

    globals:
      service_name:   "ds-api.postamat.tst.vs.market.yandex.net"
      service_port:   80
      ping:           "/boxbot/api/ds/ping"
      ping_expect:    "rstring 0;OK"

    meta:
      monrun_ignore:  true

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    be1:
      - name: "YP@testing_market_postamat-ds-api.postamat-ds-api@local"
        dns_resolvers: yandex-ns
        resolve: false
    be3:
      - name: "YP@testing_market_postamat-ds-api.postamat-ds-api"
        dns_resolvers: yandex-ns
        resolve: false

# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter
    globals:
      service_name:   "infor.wms-load.tst.vs.market.yandex.net"
      service_port:   80
      ping:           "/scprd/hc/cluster_ping"
      ping_expect:    "rstring 0;OK"
      timeout_server: "1h"
    meta:
      monrun_ignore:  yes
    options:
      allbackups:     true

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
      - name: "%market_wms-load-test-primary"
      - name: "%market_wms-load-test-secondary"
        backup: backup


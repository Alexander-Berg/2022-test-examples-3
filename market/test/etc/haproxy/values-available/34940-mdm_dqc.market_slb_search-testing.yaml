# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter
    globals:
      service_name:   mdm-dqc.tst.vs.market.yandex.net
      listen_port:    80
      service_port:   443
      ping:           "/health"
      ping_expect:    "status 200"
    meta:
      monrun_ignore:  yes

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
      - name: "YP@testing_market_mbo-mdm-dq.mdm-dqc"
        sort_order: shuffled
        dns_resolvers: "yandex-ns"
        resolve: false

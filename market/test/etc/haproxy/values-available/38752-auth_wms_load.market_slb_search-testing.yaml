# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb_triple_waterfall.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter

    be1_globals:
      timeout_connect: 25

    globals:
      service_name:   "auth.wms-load.tst.vs.market.yandex.net"
      service_port:   80
      ping:           "/auth/hc/ping"
      timeout_server: "180s"

    meta:
      monrun_ignore:  true

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    be1:
      - name: "%market_wms-load-test@local"
    be3:
      - name: "%market_wms-load-test"

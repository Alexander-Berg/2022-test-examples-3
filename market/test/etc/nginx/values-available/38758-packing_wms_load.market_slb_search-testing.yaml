# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb.tpl

# Значения, доступные в шаблоне
values:
  default:
    globals:
      service_name:       packing.wms-load.tst.vs.market.yandex.net
      proxy_read_timeout: 3600s
      proxy_send_timeout: 3600s
      web_sockets: true

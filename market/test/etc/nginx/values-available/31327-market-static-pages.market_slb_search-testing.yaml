# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb.tpl

# Значения, доступные в шаблоне
values:
  default:
    globals:
      keepalive_timeout:  60s
      service_name:       market-static-pages.tst.vs.market.yandex.net
      ssl_http_redirect:  true
      ssl:                yes
      upstream_port:      443

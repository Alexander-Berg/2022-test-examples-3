# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb.tpl

# Значения, доступные в шаблоне
values:
  default:
    globals:
      service_name:       mbi-partner.tst.vs.market.yandex.net
      ssl:                yes
      ssl_http_redirect:  true
      upstream_port:      38271

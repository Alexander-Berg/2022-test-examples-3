# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb.tpl

# Значения, доступные в шаблоне
values:
  default:
    globals:
      service_name: aliasmaker.tst.vs.market.yandex.net
      service_port: 35230
      log_name: aliasmaker.tst.vs

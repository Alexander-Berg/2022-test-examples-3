# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb.tpl

# Значения, доступные в шаблоне
values:
  default:
    globals:
      proxy_read_timeout: 600s
      proxy_send_timeout: 600s
      service_name:       autogen-api.tst.vs.market.yandex.net
      service_port:       34540
      upstream_keepalive: 4

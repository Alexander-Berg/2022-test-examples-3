# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb.tpl

# Значения, доступные в шаблоне
values:
  default:
    globals:
      service_name:       mbo-card-api-proxy.tst.vs.market.yandex.net
      proxy_read_timeout: 600s
      proxy_send_timeout: 600s


# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb.tpl

# Значения, доступные в шаблоне
values:
  default:
    globals:
      proxy_read_timeout: 1h
      proxy_send_timeout: 1h
      service_name:       mbo-cms-exporter.tst.vs.market.yandex.net
      ssl:                yes
      ssl_http_redirect:  true
      upstream_port:      80

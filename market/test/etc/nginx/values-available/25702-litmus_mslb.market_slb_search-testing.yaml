# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb.tpl

# Значения, доступные в шаблоне
values:
  default:
    globals:
      service_name:       litmus-mslb.tst.vs.market.yandex.net
      upstream_port:      8080
      ssl:                yes
      ssl_http_port:      80
      access_log:         true
      proxy_read_timeout: 300ms

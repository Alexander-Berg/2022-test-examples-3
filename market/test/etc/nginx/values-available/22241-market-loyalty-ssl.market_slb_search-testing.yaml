# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb.tpl

# Значения, доступные в шаблоне
values:
  default:
    globals:
      service_name:       market-loyalty-ssl.tst.vs.market.yandex.net
      service_port:       35816
      ssl:                yes
      proxy_read_timeout: 60s
      proxy_send_timeout: 60s

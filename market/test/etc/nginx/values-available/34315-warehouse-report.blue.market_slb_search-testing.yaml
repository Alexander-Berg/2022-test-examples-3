# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb.tpl

# Значения, доступные в шаблоне
values:
  default:
    globals:
      retry_limit:        2
      retry_on:           http_503
      service_name:       warehouse-report.blue.tst.vs.market.yandex.net
      service_port:       17051
      upstream_name:      report_tst_vs_market_yandex_net-17050
      upstream_skip:      true

# Если меняете тут данные, то нужно измененный файл загрузить в sandbox и обновить ресурс в postgresql.conf.ya.make.inc
# Загрузить можно через ya upload postgresql.conf --ttl=INF
max_prepared_transactions = 200
max_connections = 800

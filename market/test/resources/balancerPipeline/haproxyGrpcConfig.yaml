# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: true
    templates:
      - user_mslb_triple_waterfall.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      on_error:       fastinter

    be1_globals:
      timeout_connect: 25

    globals:
      grpc:           true
      service_name:   "test-service-grpc.name.tst.vs.market.yandex.net"
      listen_port:    55555
      service_port:   55556

    meta:
      monrun_ignore:  true

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    be1:
      - name: "%market_slb_search-stable@local"
        grpc_port: 55557
    be3:
      - name: "%market_slb_search-stable"
        grpc_port: 55557

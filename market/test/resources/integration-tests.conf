include classpath("logs.conf")

parsers {
  //geodata_path = "/var/cache/geobase/geodata4.bin"
  //ipreg_path = "/var/cache/geobase/layout.json"
  useNativeLookup = false
  geodataCachePath = "./geodata_versions/"
  geodataHdfsPath = "./geodata_versions/"
  ipregCachePath = "./ipreg_versions/"
  ipregHdfsPath = "./ipreg_versions/"
}

graphite {
  host = "localhost"
  port = 42000
}

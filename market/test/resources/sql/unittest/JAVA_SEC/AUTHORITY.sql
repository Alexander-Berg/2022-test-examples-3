CREATE TABLE JAVA_SEC.AUTHORITY (
  ID NUMBER NOT NULL,
  NAME VARCHAR(320) NOT NULL,
  DOMAIN_ID NUMBER NOT NULL,
  DSC VARCHAR(2048)
);

CREATE UNIQUE INDEX JAVA_SEC.AUTHORITY_PK ON JAVA_SEC.AUTHORITY (ID);

ALTER TABLE JAVA_SEC.AUTHORITY ADD CONSTRAINT AUTHORITY_PK PRIMARY KEY (ID);

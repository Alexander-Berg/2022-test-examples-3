spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:liquibase/db-changelog.xml

# Forward embedded postgres settings
shops_web.billing.jdbc.url=${sql.url}
shops_web.billing.username=${sql.userName}
shops_web.billing.password=${sql.password}
shops_web.billing.driver=org.postgresql.Driver

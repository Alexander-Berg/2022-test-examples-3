# Generated by tsum-pipeline

# Параметры обработки
params:
  default:
    resolve: yes
    templates:
      - user_mslb_http.tpl

# Значения, доступные в шаблоне
values:
  default:
    defaults:
      check:          check observe layer7
      inter:          2000
      error_limit:    5
      on_error:       fastinter
    globals:
      service_name:   wms-ekb.vs.market.yandex.net
      service_port:   80
      listen_port:    8080
      ping:           "/ping"
      ping_expect:    "status 200"
      timeout_server: "1h"
    meta:
      monrun_ignore:  true

# Именованные списки серверов, доступные в шаблоне
servers:
  default:
    main:
    - name: "%market_wms-app-stable-dc-ekb-primary"
    - name: "%market_wms-app-stable-dc-ekb-secondary"
      backup: backup

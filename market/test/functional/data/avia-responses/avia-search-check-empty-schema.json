{
  "title": "Avia search check, empty response schema",
  "type": "object",
  "required": [
    "avia"
  ],
  "properties": {
    "avia": {
      "type": "object",
      "required": [
        "type",
        "requestCount"
      ],
      "properties": {
        "type": {
          "enum": ["check", "finish"]
        },
        "requestCount": {
          "type": "number",
          "minimum": 0
        }
      }
    }
  }
}
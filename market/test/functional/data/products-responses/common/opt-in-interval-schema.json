{
  "title": "first updated request schema",
  "type": "object",
  "required": ["rules"],
  "properties": {
    "rules": {
      "type": "array",
      "items": {
        "type": "string",
        "minItems": 1,
        "maxItems": 1,
        "pattern": "opt-in-interval"
      }
    }
  }
}
{
    "title": "settings with opt-out schema",
    "type": "object",
    "required": [
        "applicationName",
        "needShowOptIn"
    ],
    "properties": {
        "applicationName": {
            "type": "string"
        },
        "needShowOptIn": {
            "enum": [
                true
            ]
        }
    }
}
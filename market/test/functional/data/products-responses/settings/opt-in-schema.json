{
    "title": "settings with opt-in schema",
    "type": "object",
    "required": [
        "applicationName",
        "needShowOptIn",
        "optOutNotAcceptByDefault"
    ],
    "properties": {
        "applicationName": {
            "type": "string"
        },
        "needShowOptIn": {
            "enum": [
                true
            ]
        },
        "optOutNotAcceptByDefault": {
            "enum": [
                true
            ]
        }
    }
}
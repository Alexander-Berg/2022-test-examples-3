'use strict';

module.exports = {
    OPT_OUT_ACCEPTED: {
        body: require('./opt-out-accetped-request.json')
    },

    OPT_OUT_REJECTED: {
        body: require('./opt-out-rejected-request.json')
    }
};
/**
 * @fileoverview
 *
 * Ценность файла сейчас околонулевая (ненулевой обладает последняя строка).
 * Оставлен для поддержки с Мандрелем, который запускает содержимое файла и домерживает его при передаче
 * --ext-env=multitesting
 */

module.exports.hosts = {};

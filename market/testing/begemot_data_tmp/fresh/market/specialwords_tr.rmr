def stop = [? gzt="stopwords"];
def price = [? gzt="pricewords"];
def param = [? gzt="paramwords"];
def opinion = [? gzt="opinionwords"];
def white = [? gzt="whitewords"];
def minus = [? gzt="minuswords"];
rule stop = (<stop> $stop);
rule stop =  .+ (<stop> fiyat listesi);
rule stop =  .+ (<stop> fiyat listeleri);
rule stop = (<stop> fiyat listeleri) .+ ;
rule stop = (<stop> fiyat listesi) .+ ;
rule stop = (<stop> kaç para) .+ ;
rule stop =  .+ (<stop> ne kadar);
rule stop = (<stop> ne kadar) .+ ;
rule stop =  .+ (<stop> satın alıyoruz);
rule stop = (<stop> satın alıyoruz) .+ ;
rule stop =  .+ (<stop> satın alma);
rule stop = (<stop> satın alma) .+ ;
rule stop =  .+ (<stop> satın almak);
rule stop = (<stop> satın almak) .+ ;
rule stop =  .+ (<stop> seçim yapmak);
rule stop = (<stop> seçim yapmak) .+ ;
rule stop =  .+ (<stop> ve fiyatlari);
rule stop =  .+ (<stop> ve fiyatları);
rule stop =  .+ (<stop> almak istiyorum);
rule stop =  .+ (<stop> kaç lira);
rule stop = (<stop> kaç lira) .+ ;
rule stop =  .+ (<stop> en ucuzunu);
rule stop = (<stop> en ucuzunu) .+ ;
rule stop =  .+ (<stop> en ucuzu);
rule stop = (<stop> en ucuzu) .+ ;
rule stop =  .+ (<stop> kaç para);
rule stop =  .+ (<stop> en ucuz);
rule stop = (<stop> en ucuz) .+ ;
rule stop =  .+ (<stop> modelleri ve fiyatları);
rule stop =  .+ (<stop> satın al);
rule stop =  .+ (<stop> al);
rule stop =  .+ (<stop> kaç tl);
rule price = (<price> $price);
rule price = (<price> seçim yapmak) .+ ;
rule price =  .+ (<price> seçim yapmak);
rule price = (<price> satın almak) .+ ;
rule price = (<price> satın alma) .+ ;
rule price =  .+ (<price> satın almak);
rule price =  .+ (<price> satın alma);
rule price = (<price> satın alıyoruz) .+ ;
rule price =  .+ (<price> satın alıyoruz);
rule price = (<price> satın al) .+ ;
rule price =  .+ (<price> satin al);
rule price =  .+ (<price> almak);
rule price =  .+ (<price> al);
rule price = (<price> satin al) .+ ;
rule param = (<param> $param);
rule opinion = (<opinion> $opinion);
rule white = (<white> $white);
rule minus = (<minus> $minus);
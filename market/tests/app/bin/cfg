Core {
    Log {
        Level: DEBUG

        # comment for writting to stdout
        Target {
            FilePath: "logs/app/app.log"
        }
    }

    Identity {
        Name: "app"
    }

    Feature {
        IncludeServerNameInSignals: true
    }

    TraceLog {
        Target {
            FilePath: "logs/app/app-trace.log"
        }
    }
}

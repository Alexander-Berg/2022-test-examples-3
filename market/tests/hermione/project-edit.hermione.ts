import {expect} from 'chai';

describe.skip('Форма редактирования проекта', () => {
    it('Проверка заполнености существующими данными', async function () {
        expect(true).to.equal(true);
    });

    it('Валидация полей', async function () {
        expect(true).to.equal(true);
    });
});
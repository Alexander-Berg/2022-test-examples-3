export enum EPlatform {
  DESKTOP = 'desktop',
  PHONE = 'phone',
}

export enum EPage {
  STORY = 'story',
  MAIN = 'main',
  RUBRIC = 'rubric',
}

export enum EService {
  NEWS = 'news',
  SPORT = 'sport',
}

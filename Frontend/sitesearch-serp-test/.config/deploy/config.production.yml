type: ydeploy
release:
  ydeploy:
    stageId: sitesearch-serp-test_production
    stage:
      spec:
        deploy_units:
          DeployUnit1:
            images_for_boxes:
              box_docker:
                tag: ${DOCKER_IMAGE_TAG}
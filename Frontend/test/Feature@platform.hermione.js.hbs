'use strict';

const PO = require('./{{name}}.page-object')('{{platform}}');

specs('Feature', function() {
    it('Внешний вид', async function() {
        await this.browser.yaOpenSerp(
            {
                text: 'текст',
                data_filter: '',
                rearr: '',
            },
            PO.{{camelCase name}}(),
        );

        //await this.browser.assertView('plain', PO.{{camelCase name}}());
    });

    /* ... */
});

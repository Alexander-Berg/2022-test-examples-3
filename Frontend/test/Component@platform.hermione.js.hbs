'use strict';

const PO = require('./{{pascalCase name}}.page-object')('{{platform}}');

specs('{{pascalCase name}}', async function() {
    it('Внешний вид', async function() {
        const { browser } = this;

        await browser.yaOpenComponent('{{lowerCase name}}-{{kebabCase platform}}', '');
        //await this.browser.assertView('plain', PO.{{camelCase name}}());
    });
});

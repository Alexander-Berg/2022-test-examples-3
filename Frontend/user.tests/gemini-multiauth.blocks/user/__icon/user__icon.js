BEM.DOM.decl('user', {

    /**
     * Фиксируем аватарку для нужд тестирования.
     *
     * @private
     * @override
     */
    _getIconPath: function() {
        return '../../../common.blocks/user/user.tests/gemini.blocks/avatar/avatar.png';
    }
});

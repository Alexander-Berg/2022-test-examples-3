{
  "qloud": {
    "host": "qloud-ext.yandex-team.ru",
    "id": "voice-ext.paskills-int.testing",
    "components": {
      "api": {
        "image": {
          "repo": "registry.yandex.net/paskills/api",
          "tag": "{{#if VCS.tag}}{{VCS.tag}}{{else}}dev{{/if}}"
        }
      },
      "api-mimino": {
        "image": {
          "repo": "registry.yandex.net/paskills/api",
          "tag": "{{#if VCS.tag}}{{VCS.tag}}{{else}}dev{{/if}}"
        }
      }
    }
  }
}

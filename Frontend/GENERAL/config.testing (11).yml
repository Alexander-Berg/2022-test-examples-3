type: ydeploy
release:
  ydeploy:
    stageId: safety_testing
    stage:
      spec:
        deploy_units:
          safety_testing_du:
            images_for_boxes:
              safety_testing_box:
                tag: ${DEPLOY_VERSION}

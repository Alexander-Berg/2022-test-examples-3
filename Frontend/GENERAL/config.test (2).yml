type: ydeploy
release:
  ydeploy:
    stageId: qachallenge-bugs-test
    stage:
      spec:
        deploy_units:
          deployUnit1:
            images_for_boxes:
              box1:
                tag: ${DEPLOY_VERSION}

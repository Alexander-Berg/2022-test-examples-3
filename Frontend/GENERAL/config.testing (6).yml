type: ydeploy
release:
  ydeploy:
    stageId: tools_goals_testing
    stage:
      spec:
        deploy_units:
          main:
            images_for_boxes:
              main-container:
                tag: ${DOCKER_IMAGE_TAG}

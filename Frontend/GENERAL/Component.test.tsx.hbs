import React from 'react';
import renderer from 'react-test-renderer';

import { {{pascalCase name}} } from '.';

describe('Jest snapshot тесты на компонент {{pascalCase name}}', () => {
    it('Тест', () => {
        const {{pascalCase name}}Component = renderer.create(
            <{{pascalCase name}}/>).toJSON();

        expect({{pascalCase name}}Component).toMatchSnapshot();
    });
});

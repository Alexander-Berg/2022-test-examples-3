import { someTestFunction } from './someTestFunction';

describe('{{pascalCase name}}.utils', function() {
    describe('someTestFunction', function() {
        it('testFunction should do sth', function() {
            const result = 2;

            expect(someTestFunction()).toEqual(result);
        });
    });
});

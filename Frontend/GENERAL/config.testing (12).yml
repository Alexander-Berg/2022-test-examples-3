type: ydeploy
release:
  ydeploy:
    stageId: safe_testing
    stage:
      spec:
        deploy_units:
          safe_testing_du:
            images_for_boxes:
              safe_testing_box:
                tag: ${DEPLOY_VERSION}

type: ydeploy
release:
  ydeploy:
    stageId: meduza-front-testing
    stage:
      spec:
        deploy_units:
          frontend:
            images_for_boxes:
              frontend:
                tag: ${APP_VERSION}
        revision_info:
          description: 'Beta ${APP_VERSION}'

type: ydeploy
release:
  ydeploy:
    stageId: qachallenge-test
    stage:
      spec:
        deploy_units:
          deployUnit1:
            images_for_boxes:
              Box1:
                tag: ${DEPLOY_VERSION}

{% set unit = 'haproxy' %}

### define in cluster pillar
#{{ unit }}-monrun-files:
#  - /etc/monrun/conf.d/haproxy.conf
####

# * Define "haproxy-config-files" in your cluster or another pillar
#
#{{ unit }}-config-files:
#  - /etc/haproxy/haproxy.cfg


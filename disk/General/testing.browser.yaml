mongo:
  options:
    sharded_mode: false
  source_controller:
    use_mongo_localhost: true
  databases_name_map:
    legacy: sync_dev
    legacy_changelog: sync_dev_changelog
    legacy_operations: sync_dev_operations
    legacy_subscriptions: sync_dev_subscriptions
    queue: mpfs_dev
